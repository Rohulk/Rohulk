name: GitHub Readme YouTube Cards
on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - uses: DenverCoder1/github-readme-youtube-cards@main
        with:
          playlist_id: PLWKjhJtqVAbn5emQ3RRG8gEBqkhf_5vxD
